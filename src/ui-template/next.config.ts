import type { NextConfig } from "next";

// To connect to your backend, add a rewrite here:
// { source: "/api/:path*", destination: "http://localhost:8080/:path*" }
const nextConfig: NextConfig = {};

export default nextConfig;
